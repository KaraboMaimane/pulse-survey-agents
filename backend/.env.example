PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=pulse
DB_PASSWORD=pulse
DB_NAME=pulse_surveys

# Optional: only set this if Elasticsearch is running (docker-compose up brings
# it up automatically). Omit it to log to the console only.
ELASTICSEARCH_NODE=http://localhost:9200
